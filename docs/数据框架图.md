# 低空经济无人机法律智能问答系统 —— 数据框架图

> 基于 **GraphRAG（知识图谱增强检索 + 大模型生成）** 的中国无人机法律法规问答系统。
> 为避免单张图过于繁琐，框架拆成三张图：
> **① 系统总览（主图，含所有环节）** → **② 离线构建管线（细节）** → **③ 在线检索细节**。
>
> 对应的 draw.io 文件 [`数据框架图.drawio`](数据框架图.drawio) 内含同名三个页面（Tab）。
> Mermaid 图可在 GitHub / Typora / VS Code(Mermaid 插件) 中直接渲染。

---

## 图① 系统总览（主图）

一张图看清所有环节：上半为**离线数据构建**（①~④），下半为**在线检索问答**（路由→召回→过滤→精排→图扩展→生成）。虚线块（🧭查询路由、🕸️图扩展）的内部实现见图②③。

```mermaid
flowchart TD
    subgraph OFFLINE ["🛠️ 离线：数据构建"]
        direction LR
        COLLECT["📥 多源采集<br/>官网/OCR/北大法宝/地方库"] --> CORPUS["📚 原始语料<br/>107部法规+531案例"]
        CORPUS --> P1["① 结构化<br/>3586单元"]
        P1 --> P2["② LLM抽取<br/>实体+语义边"]
        P2 --> P3["③ 建图→Neo4j<br/>6650节点/11557边"]
        P1 --> P4["④ 向量库<br/>Chroma 4837child+BM25"]
    end

    P3 --> READY["✅ 系统就绪：图库+向量库双库对齐<br/>(parent_id = unit_id)"]
    P4 --> READY

    subgraph ONLINE ["💬 在线：检索问答"]
        direction TB
        USER["👤 用户法律问题"] --> ROUTER["🧭 查询路由(→图③)<br/>选 sparse/dense/hybrid"]
        ROUTER --> RECALL["🔍 召回<br/>BGE-M3稠密+BM25稀疏 RRF(alpha≈0.9)"]
        RECALL --> FILTER["⏳ 时效性过滤<br/>剔除已废止/未生效"]
        FILTER --> RERANK["🎯 精排 bge-reranker-v2-m3"]
        RERANK --> EXPAND["🕸️ 图多跳扩展(→图③)<br/>Neo4j关联条款+溯源"]
        EXPAND --> CONTEXT["📄 构建上下文<br/>主条文全文+关联条款"]
        CONTEXT --> GENERATE["🤖 ⑥ LLM生成<br/>GLM-4-Flash/DeepSeek-V3<br/>只依据条文·带引用·标时效"]
        GENERATE --> OUTPUT["📺 SSE流式输出+📎引用来源"]
    end

    READY --> USER

    classDef off fill:#e8f5e8,stroke:#1b5e20,stroke-width:1px
    classDef on fill:#e1f5fe,stroke:#01579b,stroke-width:1px
    classDef ready fill:#e8f5e8,stroke:#2e7d32,stroke-width:2px
    classDef router fill:#fff9c4,stroke:#f9a825,stroke-width:1px
    classDef gen fill:#fff8e1,stroke:#f57c00,stroke-width:1px
    class COLLECT,CORPUS,P1,P2,P3,P4 off
    class USER,RECALL,FILTER,RERANK on
    class ROUTER router
    class EXPAND on
    class CONTEXT,GENERATE,OUTPUT gen
    class READY ready
```

---

## 图② 离线构建管线（细节）

四个阶段各自的脚本与产物；虚线为跨阶段的数据流转，红线为 GraphRAG 的**双库对齐关键**。

```mermaid
flowchart LR
    subgraph S ["① 结构化 (1_结构化/)"]
        direction TB
        S_RAW["📦 chunks_all.json<br/>3594条 chunk"] --> S_UNIT["🧩 structure.py<br/>一条一单元·长条不碎裂<br/>跨源去重·跳草案"]
        S_UNIT --> S_NORM["🔧 normalize_meta.py<br/>效力层级/时效性归一"]
        S_NORM --> S_OUT["📄 units_normalized.json<br/>3586单元/107部"]
    end

    subgraph E ["② 实体关系抽取 (2_抽取/)"]
        direction TB
        E_STRUCT["🏗️ build_structural.py<br/>结构边:属于"] --> E_OUT["📄 nodes/edges_llm.json<br/>语义边8392"]
        E_REGEX["🔤 build_regex_edges.py<br/>正则边:引用/定义/术语"] --> E_OUT
        E_LLM["🧠 build_llm_extract.py<br/>DeepSeek-V3抽取<br/>术语/机型/主体/场景/罚则"] --> E_VOCAB["📖 vocab.py canon()<br/>受控词表归一"]
        E_VOCAB --> E_OUT
    end

    subgraph G ["③ 知识图谱 (3_建图/+Neo4j)"]
        direction TB
        G_BUILD["⚙️ build_graph.py<br/>合并→cypher"] --> G_CYPHER["📜 neo4j_import.cypher<br/>Entity(id)约束+索引"]
        G_CYPHER --> G_NEO["🗄️ Neo4j<br/>6650节点/11557边"]
        G_NEO --> G_TYPES["🏷️ 9类节点/6类边<br/>法规107·条文3522·机型17<br/>案例531 等"]
    end

    subgraph V ["④ 向量库 (5_向量库/)"]
        direction TB
        V_CHUNK["✂️ build_chunks.py<br/>父子分块<br/>parent4053/child4837"] --> V_EMBED["🎯 build_vectordb.py<br/>BGE-M3嵌入·1024维"]
        V_EMBED --> V_CHROMA["🧊 Chroma<br/>4837child·cosine"]
        V_CHUNK --> V_BM25["🔤 build_bm25.py<br/>jieba+领域词88→bm25.pkl"]
    end

    S_OUT -.units喂入.-> E_STRUCT
    S_OUT -.units喂入.-> V_CHUNK
    E_OUT -.节点边.-> G_BUILD
    V_CHROMA -. parent_id = unit_id 对齐 .-> G_NEO

    classDef s fill:#e8f5e8,stroke:#1b5e20
    classDef e fill:#f3e5f5,stroke:#4a148c
    classDef g fill:#e0f2f1,stroke:#00695c
    classDef v fill:#fff3e0,stroke:#f57c00
    class S_RAW,S_UNIT,S_NORM,S_OUT s
    class E_STRUCT,E_REGEX,E_LLM,E_VOCAB,E_OUT e
    class G_BUILD,G_CYPHER,G_NEO,G_TYPES g
    class V_CHUNK,V_EMBED,V_CHROMA,V_BM25 v
```

---

## 图③ 在线检索细节（路由 + 召回 + 图扩展）

主图中「🧭查询路由」和「🕸️图扩展」两个虚线块的内部展开。

```mermaid
flowchart TD
    USER["👤 用户查询"] --> ROUTER["🧭 查询路由分析<br/>意图/机型/时效/是否需多跳"]
    ROUTER --> ROUTE{"🎯 路由决策"}
    ROUTE -->|精确条号/关键词| M_SPARSE["🔤 sparse<br/>纯BM25·免key保底"]
    ROUTE -->|语义/概念查询| M_DENSE["📊 dense<br/>纯向量语义"]
    ROUTE -->|复杂/关系查询| M_HYBRID["🔍 hybrid(默认)<br/>双路融合"]

    M_SPARSE --> RECALL
    M_DENSE --> RECALL
    M_HYBRID --> RECALL

    subgraph RF ["🔍 混合召回 (search.py)"]
        direction TB
        RECALL[" "] --> R_DENSE["📊 稠密召回<br/>BGE-M3→Chroma"]
        RECALL --> R_SPARSE["🔤 稀疏召回<br/>BM25关键词"]
        R_DENSE --> R_RRF["⚖️ RRF融合 alpha≈0.9<br/>稠密优先(语料同质)"]
        R_SPARSE --> R_RRF
        R_RRF --> R_PARENT["📄 父文档回溯<br/>child→parent全文"]
    end

    R_PARENT --> POST["⏳ 时效过滤 → 🎯 精排"]

    subgraph XF ["🕸️ 图多跳扩展 (graph_expand.py)"]
        direction TB
        X_SEED["🌱 种子:主命中parent_id"] --> X_HOP["🔄 Neo4j 1跳<br/>引用(双向)+共享实体"]
        X_HOP --> X_WEIGHT["📉 稀有度降权<br/>引用×3·高频机型1/log2(2+deg)"]
        X_WEIGHT --> X_TRACE["🧭 溯源标注<br/>经《种子》第X条·共享「实体」"]
    end

    POST --> X_SEED
    X_TRACE --> CONTEXT["📄 上下文 → ⑥ LLM生成"]

    classDef router fill:#fff9c4,stroke:#f9a825
    classDef recall fill:#e1f5fe,stroke:#01579b
    classDef expand fill:#fce4ec,stroke:#880e4f
    classDef gen fill:#fff8e1,stroke:#f57c00
    class ROUTER,ROUTE,M_SPARSE,M_DENSE,M_HYBRID router
    class RECALL,R_DENSE,R_SPARSE,R_RRF,R_PARENT,POST recall
    class X_SEED,X_HOP,X_WEIGHT,X_TRACE expand
    class CONTEXT gen
```

---

## 数据流说明

### 离线（图②）

| 阶段 | 目录 | 输入 → 输出 | 关键产物 |
|------|------|-------------|----------|
| **① 结构化** | `1_结构化/` | chunks_all(3594) → 结构化单元 | `units_normalized.json` 3586单元/107部 |
| **② LLM抽取** | `2_抽取/` | 单元 → 实体+关系 | 术语/机型/主体/场景/罚则 + 语义边8392 |
| **③ 建图** | `3_建图/`+Neo4j | 节点边 → cypher → 图库 | **Neo4j 6650节点/11557边** |
| **④ 向量库** | `5_向量库/` | 单元 → 父子分块 → 嵌入 | **Chroma 4837 child** + BM25 |

> **双库对齐**：向量库 `parent_id` == 图库 `Article.id` == `unit_id`，是「向量召回 ↔ 图扩展」合流的连接键。

### 在线（图③）

```
用户查询
  → 🧭 查询路由：判定意图/机型/时效，选召回策略
      · 精确条号/关键词 → sparse(纯BM25·免key保底)
      · 语义/概念查询   → dense(纯向量)
      · 复杂/关系查询   → hybrid(双路融合，默认)
  → 双路召回 BGE-M3稠密(Chroma)+BM25稀疏 ──RRF融合(alpha≈0.9)
  → 时效性过滤(剔除已废止/未生效) → bge-reranker 精排
  → Neo4j 图多跳扩展(引用/共享实体，稀有度降权，带溯源)
  → 构建上下文 → LLM生成(带引用/标时效)
```

---

## 三种模型分工

| 环节 | 模型 | 作用 |
|------|------|------|
| 召回嵌入 | **BGE-M3**（1024维，SiliconFlow API） | 稠密语义召回 |
| 精排 | **bge-reranker-v2-m3**（cross-encoder） | 二阶段精排 |
| 生成 | **GLM-4-Flash / DeepSeek-V3** | 依据条文出带引用答案 |

## 关键技术参数

| 环节 | 参数 |
|------|------|
| 语料规模 | 107部法规 + 531案例 |
| 结构化单元 | 3586 单元 |
| 知识图谱 | 6650 节点 / 11557 边（9类节点 / 6类边） |
| 父子分块 | parent 4053 / child 4837 |
| 嵌入模型 | BGE-M3，1024 维，cosine |
| 融合算法 | RRF，alpha≈0.9 稠密优先 |
| 图扩展 | Neo4j 1跳，引用边×3，高频实体 1/log2(2+deg) 降权 |
| 时效过滤 | 现行有效 / 已废止 / 已修改 / 未生效 四态 |
